{
  "name": "garden-bloom-3d",
  "version": "1.0.0",
  "description": "Garden Bloom 3D — multiplayer farming game",
  "private": true,
  "scripts": {
    "server": "node server/server.js",
    "client": "npm run dev --prefix garden-3d",
    "install:all": "npm install --prefix server && npm install --prefix garden-3d"
  }
}
